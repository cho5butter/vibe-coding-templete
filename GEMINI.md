# プロジェクトルール — Gemini CLI / Antigravity

仕様駆動開発（Spec-Driven Development）テンプレート。
詳細なルールは `.antigravity/rules.md` および `spec/workflow.md` を参照すること。

---

## ⛔ セッション開始時の必須手順（スキップ禁止）

**すべてのセッションは以下の3ステップから始めること。例外なし。**

1. `git fetch` でリモートを確認する。リモートがローカルより進んでいる場合は取り込んでから作業を開始する（fast-forward可能なら取り込み、conflictや未コミット変更がある場合は取り込み方針をユーザーに確認する）
2. `spec/status.md` を読む
3. 現在フェーズ・直近の状況・次のアクションをユーザーに伝え、認識を合わせる

セッション終了時には `spec/status.md` の「直近の状況」と「次のアクション」を必ず更新すること。

---

## ⛔ プロジェクト原則（全フェーズで意識すること）

詳細は `spec/workflow.md` の「プロジェクト原則」を参照。

1. **DDD（ドメイン駆動設計）**: ユビキタス言語・境界づけられたコンテキスト・レイヤー分離を厳守
2. **OWASP Top 10**: 全フェーズで意識する（インジェクション/認証認可不備/機微情報露出/XSS/SSRF 等）
3. **画面表示 100ms 以下を目標**: 非機能要件のデフォルト目標
4. **`grill-with-docs` スキル**: 要件定義・設計フェーズの開始時に**採用可否を選択肢でユーザーに確認**し、実施する場合は曖昧さを徹底的に深掘り
5. **実行環境・計測の運用**: サンドボックスが使えるツールは活用する（推奨）／実装フェーズは計画単位で `git worktree` を積極活用する／NFR実測はループエンジニアリング（ゴールコマンドによる反復測定）で行う（推奨）

## ⛔ フェーズゲート — 絶対に破ってはならないルール

プロジェクトは **要件定義 → 設計 →（デザイン）→ 計画 → 実装** の順にのみ進む。デザインフェーズはオプション。

```
フェーズ1: 要件定義  →（承認）→  フェーズ2: 設計
フェーズ2: 設計      →（承認＋デザイン実施確認）→ フェーズ3: デザイン（オプション）
フェーズ3: デザイン  →（承認）→  フェーズ4: 計画
フェーズ4: 計画      →（承認）→  フェーズ5: 実装
```

**設計フェーズ承認直後に必ず確認すること:**
> 「デザインフェーズ（UI/UXモック作成）を実施しますか？」
- YES → フェーズ3（デザイン）へ進み、画像生成 / Figma / Pencil でモック作成
- NO → フェーズ3を省略してフェーズ4（計画）へ進む

### 各フェーズで「やってよいこと」と「やってはいけないこと」

| フェーズ | やってよいこと | やってはいけないこと |
|---------|--------------|-------------------|
| 要件定義 | `spec/requirements.md` の作成・質問・確認・`grill-with-docs` 実施 | 設計・計画・コード実装 |
| 設計 | `spec/design.md` の作成・図の作成・DDD/OWASP/100ms設計・`grill-with-docs` 実施 | コード実装・計画の作成 |
| デザイン | 画像生成 / Figma / Pencil によるUIモック作成 | コード実装・計画の作成 |
| 計画 | `spec/plan.md` の作成・タスク分割 | コード実装 |
| 実装 | コーディング・テスト・自己レビュー・コミット | specを勝手に変更する |

### フェーズ移行の手順（必須）

次のフェーズに進む前に、以下を**必ず**実行すること:

1. 現フェーズの成果物（`spec/requirements.md` / `spec/design.md` / `spec/plan.md`）をユーザーに提示する
2. ユーザーから**明示的な承認の言葉**をもらう（「OK」「承認」「進めて」等）
3. `spec/status.md` を更新する
4. **一旦作業を停止し、「このまま次のフェーズの作業を続けてよいか」をユーザーに確認する**
5. ユーザーが続行を指示した場合のみ、次のフェーズの作業を開始する

**ユーザーが明示的に承認しない限り、次のフェーズに進んではならない。**
**承認を得ても、ステータス更新後は必ず一旦停止し、続行可否を確認してから作業を再開すること（承認＝即着手ではない）。**

---

## ⛔ 実装フェーズに入る前のチェックリスト（全項目必須）

実装を開始する前に、以下をすべて確認すること:

- [ ] `spec/requirements.md` を読んだ
- [ ] `spec/design.md` を読んだ
- [ ] `spec/plan.md` の当該計画を確認した
- [ ] ユーザーが計画フェーズを承認済みである（`spec/status.md` で確認）
- [ ] テストを先に書く準備ができている（TDD）

## ⛔ 実装中・完了後のドキュメント更新ルール（必須）

**各タスクを完了したら即座に該当チェックボックスを更新すること。まとめて後から更新することは禁止。**

| ファイル | チェックボックスの意味 | 更新タイミング |
|---------|-------------------|-------------|
| `spec/plan.md` | 実装タスクの完了状態 | 各タスク実装完了直後 |
| `spec/status.md` | プロジェクト初期化タスクの完了状態 | 各初期化タスク完了直後 |
| その他 `spec/` 内の MD | フェーズ・項目の完了状態 | 該当作業完了直後 |

更新手順:
1. タスクの実装・確認が完了する
2. **その場で即座に** 該当する `[ ]` を `[x]` に書き換える
3. 変更をコミットに含める（MDファイルのみのコミットも可）

---

## 絶対ルール

1. **フェーズゲート厳守**: 要件定義→設計→（デザイン）→計画→実装の順に進行し、各フェーズ間でユーザーの明示的な承認を得ること
2. **spec参照必須**: 実装前に `spec/requirements.md` と `spec/design.md` を必ず読むこと
3. **spec変更はユーザー合意必須**: `spec/` 内のファイルを勝手に変更してはならない
4. **TDD厳守**: 要件からテストを先に書き、RED を確認してから実装する（詳細は `spec/workflow.md` のTDDガイドライン参照）
5. **品質ゲート必須**: `--no-verify` 禁止。品質ゲートの失敗は根本原因を修正する
6. **自己レビュー必須**: 各計画の実装完了時（コミット前）と全計画完了時の2段階で自己レビューを実施する（詳細は `spec/workflow.md` の「自己レビュー」セクション参照）
7. **DDD/OWASP/100ms 原則**: 全フェーズで DDD・OWASP Top 10・画面表示 100ms 以下目標を意識する
8. **デザイン実施確認**: 設計フェーズ承認直後に「デザインフェーズを実施しますか？」を必ず質問する
9. **コミットにMDファイルを含める**: 計画で生成・変更されたMDファイルをコミットから除外しない
10. **コミットメッセージは日本語**: 種別は `機能` / `修正` / `改善` / `整理` / `テスト` / `文書` / `設定` / `計画`
11. **PRは `.github/PULL_REQUEST_TEMPLATE.md` に従う**: 概要・チェックリスト・テスト計画を記載
12. **ドキュメントチェックボックス更新必須**: タスク完了直後に、該当するすべてのチェックボックスを `[ ]` から `[x]` に更新すること（対象: `spec/plan.md`・`spec/status.md` の初期化チェックリスト等）
13. **承認後の停止・続行確認**: フェーズ承認を得たら `spec/status.md` を更新し、**一旦作業を停止して「このまま続けてよいか」をユーザーに確認** する。続行指示を得てから次の作業に着手する（承認＝即着手ではない）
14. **レビュー要点の説明**: 自己レビュー・脆弱性レビューの後は、チェックリストで完結させず、**要点（重要な発見・リスク・取った対応）をユーザーにわかりやすく説明** する（詳細は `spec/workflow.md` の「レビュー結果の説明ルール」参照）
15. **完了後の次作業提示**: 作業（フェーズ・計画・タスク）完了後は、**次の作業を具体的に示す**。次の作業がなければ**完了の旨を明示** する（詳細は `spec/workflow.md` の「作業完了後の報告ルール」参照）
16. **`grill-with-docs` 採用可否を選択肢で確認**: 要件定義・設計フェーズ開始時に、`grill-with-docs` を実施するかどうかを選択肢ダイアログで提示し、ユーザーに選んでもらう（勝手に実施・省略しない）
17. **確認は選択肢ダイアログ優先**: ユーザーへの確認・質問は、可能な限り選択肢（選択ダイアログ）形式で提示する（詳細は `spec/workflow.md` の「確認・質問の提示方法」参照）
18. **worktreeの活用**: 実装フェーズでは計画単位で専用の `git worktree` を作成して作業し、完了後に整理する（詳細は `spec/workflow.md` の「計画実行のワークフロー」参照）

---

## よくある違反パターン（絶対にやらない）

| 違反 | 正しい対応 |
|-----|-----------|
| 要件定義が終わっていないのに設計を始める | 要件定義を完了しユーザーの承認を得てから設計に進む |
| 設計が終わっていないのにコードを書く | 設計を完了しユーザーの承認を得てから計画・実装に進む |
| ユーザーに聞かずに次のフェーズに進む | 必ずフェーズ完了を宣言し、承認をもらってから進む |
| specを読まずに実装を開始する | 実装前に必ず `spec/requirements.md` と `spec/design.md` を参照する |
| テストを後から書く | TDDサイクルを厳守。テストが RED であることを確認してから実装 |
| `--no-verify` でコミットする | 品質ゲートの失敗は根本原因を修正する |
| タスク完了後にチェックボックスを更新しない | 実装完了と同時に `spec/plan.md` などの該当チェックボックスを `[x]` に更新する |
| 自己レビューをスキップしてコミット | 計画単位の自己レビューチェックリストを必ず実施してからコミット |
| 設計後にデザイン実施可否を確認しない | 設計承認直後に必ず「デザインフェーズを実施しますか？」を質問 |
| DDDレイヤー分離を崩す | ドメイン層をフレームワーク・DBに依存させない。境界づけられたコンテキストを守る |
| 画面が遅いまま放置 | 100ms目標を意識し、楽観的UI・スケルトン等で知覚レイテンシを下げる |
| リモートを確認せずセッションを開始する | セッション開始時に必ず `git fetch` でリモートを確認し、進んでいれば取り込んでから作業する |
| 計画をmainや共有ブランチ上で直接進めてしまう | 計画開始時に専用の `git worktree` を作成し、そこで作業する |
| NFR達成を一度きりの手動確認で済ませる | ゴールコマンド等を用いたループエンジニアリングで反復実測し、達成を裏付ける |
| 承認後に確認なくそのまま作業を続行する | ステータス更新後に一旦停止し、続行してよいかを確認してから着手する |
| レビュー結果を説明せず先に進む | レビューの要点（発見・リスク・対応）をわかりやすく説明する |
| 作業完了後に次の作業を示さない | 完了報告とあわせて次の作業を提示する。なければ完了の旨を明示する |

---

## パッケージバージョン選定ルール

- ライブラリ・フレームワークのバージョン指定時は、**今日の日付の3日前時点で公開済みの最新安定版**を採用する
- リリース直後の不安定版を避けるための予防策
- 例: 今日が `2026-05-17` なら `2026-05-14` 時点で公開済みの最新安定版を選ぶ
- `--before` 相当の指定ができないパッケージマネージャでは、レジストリ（npm registry / PyPI / pkg.go.dev 等）で公開日を確認する

## 実装計画末尾の脆弱性レビュー（必須）

`spec/plan.md` のすべての計画完了後、リリース/マージ前に **必ず** 以下の脆弱性レビューを実施する:

1. **実装の脆弱性レビュー**: OWASP Top 10（インジェクション、XSS、認証/認可不備、機微情報露出、SSRF 等）、入力検証・出力エスケープ・認可チェックの抜け漏れ、秘密情報のハードコード等
2. **パッケージの脆弱性レビュー**: 言語標準のスキャナ（`npm audit` / `pnpm audit` / `bun audit` / `pip-audit` / `govulncheck ./...` / `cargo audit` 等）を実行、GitHub Security タブ（Dependabot アラート）を確認

High / Critical は **マージ前に解消** すること。詳細は `spec/workflow.md` の「実装計画末尾の脆弱性レビュー」セクション、および `spec/plan.md` 末尾の固定計画「脆弱性レビュー」を参照。

## Dependabot（週次脆弱性チェック）

- `.github/dependabot.yml` で **毎週月曜 09:00 JST** に脆弱性スキャンが自動実行される
- 脆弱性が検出された場合、Dependabot が **更新PRを自動作成** する（GitHub Security タブにもアラート表示）
- 新規プロジェクトでは、`.github/dependabot.yml` のコメントアウトされた `package-ecosystem` を使用する技術スタックに応じて有効化すること

## 頻用コマンド

```bash
bash scripts/setup-hooks.sh    # 初回セットアップ
bash scripts/quality-gate.sh   # 品質ゲート（手動事前確認）
bash scripts/lint.sh           # リント・静的解析
bash scripts/build.sh          # ビルド確認
bash scripts/test.sh           # テスト

# devcontainer でテスト実行（推奨。サンドボックスが使える場合は積極的に利用する）
devcontainer exec --workspace-folder . bash scripts/test.sh
```

詳細は `.antigravity/rules.md` および `spec/workflow.md` を確認すること。
