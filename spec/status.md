# プロジェクト進行ステータス

<!-- このファイルはセッションをまたいでフェーズ状態を記録するためのファイルである -->
<!-- AIエージェントはセッション開始時に必ずこのファイルを参照し、現在フェーズを把握すること -->
<!-- フェーズが進んだ際は、ユーザーの承認後にこのファイルを更新すること -->

## 現在フェーズ

**フェーズ**: 要件定義
**ステータス**: 未着手

```
[ ] フェーズ1: 要件定義     ← 現在地
[ ] フェーズ2: 設計
[ ] フェーズ3: デザイン（オプション）
[ ] フェーズ4: 計画
[ ] フェーズ5: 実装
```

## フェーズ履歴

| フェーズ | 開始日 | 承認日 | 担当AI | 備考 |
|--------|--------|--------|--------|------|
| 要件定義 | <!-- YYYY-MM-DD --> | <!-- YYYY-MM-DD --> | <!-- Claude Code 等 --> | <!-- 備考 --> |

## 直近の状況

**最後に実施したこと**:
ユーザー指示に基づき、以下4点の運用ルールを `spec/workflow.md`（正本）と `CLAUDE.md` / `AGENTS.md` / `GEMINI.md` / `.antigravity/rules.md`（全エージェント設定ファイル）に横断反映した。
1. サンドボックスが使えるツールはサンドボックスを利用する（推奨事項として、devcontainer推奨と並記）
2. NFR（100ms目標等）の実測はループエンジニアリングのアプローチ（ゴールコマンドによる反復測定）で行う（自己レビュー段階2の推奨手法）
3. 実装フェーズは計画単位で `git worktree` を積極活用する（常時デフォルトの運用として絶対ルールに追加）
4. セッション開始時に `git fetch` でリモートを確認し、進んでいれば取り込む（fast-forward可能なら自動、conflict・未コミット変更時はユーザーに確認）を必須手順に追加
既存内容との矛盾は検出されなかったが、強制力・適用範囲に解釈の幅があったため、事前にユーザーへ4点の選択肢確認を行った上で反映した。

**次のアクション**:
ユーザーが更新内容を確認したら、要件定義フェーズを開始する。

**ブロッカー・懸念事項**:
<!-- 例: ターゲットユーザーの定義が曖昧。ユーザーに確認が必要 -->

## プロジェクト初期化チェックリスト

<!-- テンプレートから新規プロジェクトを作成した直後に実施するタスク -->
<!-- 完了したらチェックを入れること -->

- [ ] `README.md` をプロジェクト内容に合わせて書き換えた
- [ ] `spec/requirements.md` のTODOプレースホルダーを記述し始めた
- [ ] `spec/design.md` のTODOプレースホルダーを記述し始めた
- [ ] `spec/plan.md` のTODOプレースホルダーを記述し始めた
- [ ] `scripts/lint.sh` をプロジェクトに合わせて書き換えた
- [ ] `scripts/build.sh` をプロジェクトに合わせて書き換えた
- [ ] `scripts/test.sh` をプロジェクトに合わせて書き換えた
- [ ] `.github/dependabot.yml` で使用する `package-ecosystem` のコメントアウトを解除した
- [ ] `bash scripts/setup-hooks.sh` を実行してpre-commitフックを有効化した

## ステータス更新ルール

- フェーズが進むたびに「現在フェーズ」と「フェーズ履歴」を更新する
- 更新はユーザーの承認を得た後に行う
- 「直近の状況」はセッション終了時に必ず更新する
